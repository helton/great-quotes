version: '3.1'

services:

  mysql:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: k2j32ks4
    ports:
          - 3306:3306
    volumes:
      - ../../../../db:/docker-entrypoint-initdb.d